import React from "react";

interface GreetingProps {
    name: string
}

const GreetingRevou = ({name}: 
GreetingProps) => {  
    return (
        <div>
            <h1>Greeting component revou berlin

            </h1>
        </div>
    );
};

export default GreetingRevou;